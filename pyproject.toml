[build-system]
requires = [
    "numpy>=2.0",
    "setuptools>=42",
    "setuptools_scm",
    "wheel",
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "msprime/_version.py"
