name: Docs
on:
  pull_request:
  push:
    branches: [main]
    tags:
      - '*'
  merge_group:

permissions:
  contents: read

jobs:
  Docs:
    uses: tskit-dev/.github/.github/workflows/docs.yml@v18
    with:
      additional-apt-packages: libgsl-dev
